<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:icon="@android:drawable/ic_menu_search"
        android:id="@+id/search"
        app:showAsAction="always"
        android:orderInCategory="10"
        android:title="@string/main_search_title"
        tools:ignore="AppCompatResource" />

    <item android:icon="@android:drawable/ic_menu_add"
        android:id="@+id/addRecord"
        app:showAsAction="always"
        android:orderInCategory="100"
        android:title="@string/main_add_title"
        tools:ignore="AppCompatResource" />

    <item android:icon="@android:drawable/ic_menu_delete"
        android:id="@+id/deleteDatabase"
        app:showAsAction="never"
        android:orderInCategory="50"
        android:title="@string/main_deletedatabase_title"
        tools:ignore="AppCompatResource" />

</menu>